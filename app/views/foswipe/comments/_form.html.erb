<%= form_for([@ticket,@comment]) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content,:cols => "30", :rows => "5",:class=>"form-control" %>
  </div>
  <div class="field">
    <%= f.label :user %><br>
    <%= f.text_field :user,:class=>"form-control" %>
  </div>
  <%= f.label "Comment Attachment:" %>
		
			<%= f.file_field :attachment, :name => "comment[comment_attachments_attributes][][attachment]", :multiple => true %>
 <br> <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<%= link_to 'Back', clients_path, :class=>"btn btn-lg btn-primary" %>